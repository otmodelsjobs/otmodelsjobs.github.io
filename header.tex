\usepackage{fancyhdr}
\pagestyle{fancy}
%\newcommand{\commitHash}{$commit_hash$}  % Define commit hash as a command
% Center footer with commit hash and branch name
\fancyfoot[C]{Commit: \verb|$commit_hash$| }
\renewcommand{\footrulewidth}{0.4pt} % Optional: Adds a line above the footer
